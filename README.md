# Explore With Me - Сервис поиска событий

## Описание проекта

Explore With Me — это сервис для публикации и поиска мероприятий. Пользователи могут находить интересные события, создавать свои мероприятия, участвовать в чужих, а также комментировать события.

Сервис состоит из двух основных модулей:
- **Основной сервис (ewm-main-service)** — управление событиями, пользователями, категориями, подборками и комментариями
- **Сервис статистики (ewm-stats-service)** — сбор и хранение статистики просмотров событий

## Ключевые возможности

- Публикация и поиск событий по категориям, датам, местоположению
- Участие в событиях (запросы на участие)
- Система комментариев с модерацией
- Подсчет просмотров событий
- Управление подборками событий
- Административная панель для управления контентом

## 🚀 Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Maven 3.8+
- Java 21

### Клонирование проекта
`git clone https://github.com/nTONy4u/java-explore-with-me.git`  
`cd java-explore-with-me`



### Запуск в Docker

**Соберите проект:**
`mvn clean package -DskipTests`



**Запустите Docker и затем контейнеры:**
`docker-compose up --build`



**Сервисы будут доступны по адресам (измените на другие, если у вас заняты):**
- Основной сервис: [http://localhost:8080](http://localhost:8080/)
- Сервис статистики: [http://localhost:9090](http://localhost:9090/)
- База данных (основная): порт 5434
- База данных (статистики): порт 5432


## Запуск Postman тестов

### Тестирование фичи комментариев
1. Проверьте, что оба сервиса и обе базы данных запущены (`docker ps -a`)
2. Откройте Postman
3. Импортируйте коллекцию из файла: `postman/feature.json`
4. Выполните тесты последовательно (они сгруппированы по разделам)


## Основные эндпоинты API

### Публичные эндпоинты
`GET /events` - Поиск событий с фильтрами  
`GET /events/{id}` - Получение события по ID  
`GET /categories` - Получение категорий  
`GET /categories/{id}` - Получение категории по ID  
`GET /compilations` - Получение подборок событий  
`GET /compilations/{id}` - Получение подборки по ID  
`GET /events/{eventId}/comments` - Получение комментариев события  
`GET /events/{eventId}/comments/{commentId}` - Получение комментария с ответами  


### Приватные эндпоинты (требуют авторизации)
`GET /users/{userId}/events` - События пользователя  
`POST /users/{userId}/events` - Создание события  
`GET /users/{userId}/events/{eventId}` - Получение события пользователя  
`PATCH /users/{userId}/events/{eventId}` - Обновление события  
`GET /users/{userId}/requests` - Запросы на участие  
`POST /users/{userId}/requests` - Создание запроса на участие  
`PATCH /users/{userId}/requests/{requestId}/cancel` - Отмена запроса  
`GET /users/{userId}/events/{eventId}/requests` - Участники события  
`PATCH /users/{userId}/events/{eventId}/requests` - Обновление статуса заявок  
`GET /users/{userId}/comments` - Комментарии пользователя  
`POST /users/{userId}/comments` - Создание комментария  
`PATCH /users/{userId}/comments/{commentId}` - Обновление комментария  
`DELETE /users/{userId}/comments/{commentId}` - Удаление комментария  
`GET /users/{userId}/comments/{commentId}/can-edit` - Проверка возможности редактирования  



### Административные эндпоинты
`POST /admin/users` - Создание пользователя  
`GET /admin/users` - Получение пользователей  
`DELETE /admin/users/{userId}` - Удаление пользователя  
`POST /admin/categories` - Создание категории  
`PATCH /admin/categories/{catId}` - Обновление категории  
`DELETE /admin/categories/{catId}` - Удаление категории  
`POST /admin/compilations` - Создание подборки  
`PATCH /admin/compilations/{compId}` - Обновление подборки  
`DELETE /admin/compilations/{compId}` - Удаление подборки  
`GET /admin/events` - Поиск событий (админ)  
`PATCH /admin/events/{eventId}` - Обновление события (админ)  
`GET /admin/comments` - Комментарии для модерации  
`PATCH /admin/comments/{commentId}/moderate` - Модерация комментария  
`PATCH /admin/comments/{commentId}/restrict` - Ограничение редактирования  



### Эндпоинты статистики
`POST /hit` - Сохранение информации о запросе  
`GET /stats` - Получение статистики



## Структура базы данных

Проект использует две базы данных PostgreSQL:
- **ewm_db** — основная база для хранения событий, пользователей, комментариев
- **stats_db** — база для хранения статистики просмотров

**Основные таблицы:**
- `users` — пользователи
- `categories` — категории событий
- `events` — события
- `participation_requests` — запросы на участие
- `compilations` — подборки событий
- `comments` — комментарии к событиям
- `endpoint_hits` — статистика просмотров


## Особенности реализации

### Система комментариев
- Иерархические комментарии (основной + ответы)
- Модерация комментариев администраторами
- Ограничение времени редактирования (30 дней)
- Возможность ограничения редактирования со стороны администраторов
- Статусы комментариев: `PUBLISHED`, `PENDING`, `REJECTED`, `EDITED`

### Статистика просмотров
- Отдельный микросервис для сбора статистики
- Поддержка уникальных и не уникальных просмотров
- Фильтрация по временному диапазону
- Интеграция через HTTP клиент
________________________________________________________________________________________________________________________
**🔗 Ссылка на [PR](https://github.com/nTONy4u/java-explore-with-me/pull/3)**